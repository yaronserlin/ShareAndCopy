/**
 * Re-exports the RateLimitError component as the public entry point for
 * the `RateLimitError` component directory.
 */

import RateLimitError from './RateLimitError';
export default RateLimitError;

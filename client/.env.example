# Base URL of the backend API server, used as a fallback when
# window.SERVER_URL (injected by startup.sh) isn't set.
VITE_SERVER_URL=http://localhost:5001
VITE_API_BASE_URL=http://localhost:5001/api
